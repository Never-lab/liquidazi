# docs/

Materiale di supporto al prodotto — **non** è il codice runtime.

| Path | Cosa |
|------|------|
| `wiki/` | Living handbook (agents + player help). GitHub Wiki is a mirror — edit here first. |
| `fiscal-snapshot/` | Specchio educational delle aliquote (allineato a `src/config/fiscalYearSnapshot.ts`; non “legge live”) |
| `istat-geo.json`, `province-firms.json` | Pack sorgente / export; l’app **non** li importa (runtime = `src/config/istatGeo.json`, `src/config/provinceFirms.json`) |
| `superpowers/specs/` | Design degli slice post-MVP |
| `superpowers/plans/` | Piani di implementazione agenti / fasi |

Stato shipped e coda attiva: [`../ROADMAP.md`](../ROADMAP.md).
