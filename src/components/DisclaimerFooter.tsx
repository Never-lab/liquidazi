import styles from "./DisclaimerFooter.module.css";

export const DisclaimerFooter = () => (
  <footer className={styles.footer}>
    Modello educativo semplificato — non consulenza fiscale.
  </footer>
);
